* text=auto eol=lf

*.py   text eol=lf
*.js   text eol=lf
*.yml  text eol=lf
*.html text eol=lf
*.md   text eol=lf

# AES-GCM ciphertext: never diff or merge it.
data/leviathan.enc binary

# A single-line, ~2MB minified file regenerated by .github/workflows/sync-wiki.yml.
# Marking it generated keeps it out of the repo's language stats and stops GitHub
# and `git log -p` from rendering a useless one-line diff on every hourly sync.
data/wiki-data.json -diff -merge linguist-generated=true
