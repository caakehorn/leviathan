# Everything on this deployment now sits behind the passphrase gate
# (js/gate.js), so there is nothing here a crawler could usefully render.
#
# Advisory only, and NOT an access control — a crawler that ignores this file,
# or anyone who types a URL directly, still gets whatever is served. The
# payloads that ship as plaintext (data/*.json, js/*-data.js) stay fetchable
# regardless of what this file or the gate says. See "What the gate does not
# do" in the README.
User-agent: *
Disallow: /
